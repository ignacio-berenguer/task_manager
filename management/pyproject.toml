[project]
name = "task-manager-migration"
version = "0.1.0"
description = "Task Manager migration CLI: Excel to PostgreSQL"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.0",
    "openpyxl>=3.1.0",
    "python-dotenv>=1.0.0",
    "psycopg2-binary>=2.9.0",
]
